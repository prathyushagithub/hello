pipeline {
    agent none
    stages {
	
	stage('Non-Parallel Stage') {
	    agent {
                        label "main"
                }
        steps {
                echo 'This stage will be executed first'
                }
        }
